# Development Environment Configuration
# This file should NOT be committed to git

# Server URLs - Use Nginx gateway for web compatibility
# Web app at port 3007 making requests to port 80 (Nginx) = same-origin (just different path)
# Nginx routes /api/* to respective services internally
# Mobile can still use network config to override for direct port access if needed
BASE_URL=http://3chan.kr
ADMIN_URL=http://3chan.kr:3006
CONTENT_URL=http://3chan.kr
PROGRESS_URL=http://3chan.kr
MEDIA_URL=http://3chan.kr

# Environment Mode
ENV_MODE=development

# Debug Settings
ENABLE_DEBUG_MODE=true
