---
date: 2026-02-02
category: Mobile
title: 토스 스타일 온보딩 UI 리디자인
author: Claude Opus 4.5
tags: [onboarding, ui, design, toss-style]
priority: medium
---

# 토스 스타일 온보딩 UI 리디자인

## 개요
온보딩 화면을 토스(Toss) 앱 스타일처럼 세련되고 깔끔하게 변경했습니다. 밝은 레몬색 배경에 흰색 카드를 사용하여 더 현대적인 느낌을 주도록 했습니다.

## 주요 변경 사항

### 1. 색상 시스템 (`onboarding_colors.dart`)

**새로운 색상 팔레트:**
| 용도 | 색상 | 코드 |
|------|------|------|
| 배경 | 밝은 레몬색 | `#FFF8E1` |
| 카드 | 흰색 | `#FFFFFF` |
| 선택된 카드 | 연한 노란색 | `#FFF9E3` |
| 제목 텍스트 | 진한 검정 | `#191F28` |
| 부제목 텍스트 | 회색 | `#6B7684` |
| 버튼 | 단색 노란색 | `#FFC107` |
| 카드 테두리 | 연한 회색 | `#E5E8EB` |
| 선택 테두리 | 진한 노란색 | `#FFD54F` |

### 2. 버튼 스타일 (`onboarding_button.dart`)

**변경 전:**
- 그라데이션 (노란색 → 주황색)
- 그림자 효과
- 흰색 텍스트

**변경 후:**
- 단색 노란색 (`#FFC107`)
- 플랫 디자인 (그림자 없음)
- 어두운 텍스트 (토스 스타일)
- 모서리 14px로 약간 증가

### 3. 카드 스타일 (모든 카드 위젯)

**변경 전:**
- 회색 배경 (`#F7F9FC`)
- 선택 시에만 테두리

**변경 후:**
- 흰색 배경 + 부드러운 그림자
- 기본 연한 테두리 (`#E5E8EB`)
- 선택 시: 연한 노란 배경 + 진한 노란 테두리
- 체크마크: 노란색 원형 배경 + 흰색 체크 아이콘

### 4. 타이포그래피 (`onboarding_text_styles.dart`)

| 스타일 | 변경 사항 |
|--------|----------|
| headline1 | `w700` → `w800`, 자간 `-0.5` → `-0.8` |
| headline2 | `w700` 추가, 자간 `-0.5` |
| body1/body2 | 색상을 `textSecondary`로 통일 |
| caption | 색상을 `textTertiary`로 변경 |
| cardTitle | 새로 추가 (카드 제목용) |

### 5. 레몬 캐릭터 (`lemon_character.dart`)

- 더 선명한 노란색 그라데이션 적용
- 그림자 더 진하게 (대비 강화)
- 잎사귀 색상 더 진하게 (`#4CAF50` → `#43A047`)

### 6. 화면 배경색 (5개 화면)

모든 온보딩 화면의 `Scaffold` 배경색을 `backgroundYellow` (`#FFF8E1`)로 변경:
- `language_selection_screen.dart`
- `welcome_introduction_screen.dart`
- `level_selection_screen.dart`
- `weekly_goal_screen.dart`
- `personalization_complete_screen.dart`
- `welcome_level_screen.dart` (레거시)

## 시각적 변화

```
변경 전                           변경 후
─────────────────────────        ─────────────────────────
┌───────────────────────┐        ┌───────────────────────┐
│     ← 흰색 배경        │        │     ← 레몬색 배경      │
│                       │        │                       │
│   🍋                  │        │   🍋 ← 더 선명        │
│                       │        │                       │
│   选择语言 ← 주황색   │        │   选择语言 ← 검정     │
│                       │        │                       │
│ ┌───────────────────┐ │        │ ┌───────────────────┐ │
│ │ 회색 배경          │ │        │ │ 흰색 + 그림자      │ │
│ └───────────────────┘ │        │ └───────────────────┘ │
│                       │        │                       │
│ [=그라데이션 버튼===] │        │ [  단색 노란 버튼  ]  │
└───────────────────────┘        └───────────────────────┘
```

## 수정된 파일 목록

### 핵심 스타일 파일
1. `onboarding/utils/onboarding_colors.dart` - 색상 팔레트
2. `onboarding/utils/onboarding_text_styles.dart` - 타이포그래피

### 위젯 파일
3. `onboarding/widgets/onboarding_button.dart` - 버튼
4. `onboarding/widgets/language_selection_card.dart` - 언어 선택 카드
5. `onboarding/widgets/feature_card.dart` - 기능 카드
6. `onboarding/widgets/level_selection_card.dart` - 레벨 카드
7. `onboarding/widgets/goal_selection_card.dart` - 목표 카드
8. `onboarding/widgets/summary_card.dart` - 요약 카드
9. `onboarding/widgets/lemon_character.dart` - 레몬 캐릭터

### 화면 파일
10. `onboarding/language_selection_screen.dart`
11. `onboarding/welcome_introduction_screen.dart`
12. `onboarding/level_selection_screen.dart`
13. `onboarding/weekly_goal_screen.dart`
14. `onboarding/personalization_complete_screen.dart`
15. `onboarding/welcome_level_screen.dart`

## 하위 호환성

기존 색상 상수들은 새로운 색상으로 매핑되어 하위 호환성을 유지합니다:
- `darkBlue` → `textPrimary`
- `gray` → `textSecondary`
- `lightGray` → 유지 (레거시 코드용)

## 검증 방법

1. `flutter run`으로 앱 실행
2. 온보딩 진입하여 5개 화면 확인
3. 레몬색 배경 확인
4. 카드 선택 시 시각적 피드백 확인
5. 버튼 스타일 확인
